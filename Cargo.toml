[package]
name = "tone"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal = "0.15"
eframe = { version = "0.28", default-features = false, features = ["default_fonts", "glow"] }
egui = "0.28"

[profile.release]
opt-level = 3
lto = "thin"

[profile.dev]
opt-level = 1
